# experiments.md 是全组人共同追加的实验日志表——多人同时在表尾加行，
# 用默认的逐行 diff 合并几乎每次都会在同一处产生"假冲突"（两边都是新增，
# 不是真的互相矛盾）。union 策略让 git 自动保留双方新增的行，不再打断人。
# 代价：合并后的行序可能不是严格时间顺序、行号可能撞号，需要合并后人工扫一眼
# （这个仓库已有先例：commit 1427633 就是手动重编号撞号的实验行）。
team/experiments.md merge=union

# 留言板同样是追加型文件：多人同时在末尾追加帖子/回复，union 合并避免假冲突
team/留言板.md merge=union
