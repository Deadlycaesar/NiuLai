# 实验记录

> 每次全量评测（200 sessions）记一行。格式：改动 → 总分 + 分场景。归 E 维护，人人可追加。
> 复现：`python3 -m evaluator.local_evaluator`（评测器确定性，同代码必同分）。

| # | 日期 | 配置/改动 | TechnicalScore | HitRate@10 | MRR | MTTC | buy/browse/override/boundary (hit) |
|---|---|---|---|---|---|---|---|
| 0 | 08-29 | 官方 weak-BM25 baseline | 0.10671 | 0.125 | 0.068 | 9.81 | .238/.025/.133/.000 |
| 1 | 08-29 | 骨架 v1：状态累积+每轮带推荐+other_first 提问+约束子串排序；override=硬擦除 | 0.809 | 0.930 | 0.607 | 2.91 | .975/.975/**.667**/1.0 |
| 2 | 08-29 | override 改"降权保留"（评测器不二次吐露已披露约束，硬擦=永久丢信息）+ 排序加权（硬>软、长约束>短、精确价格+4） | 0.8525 | 0.970 | 0.664 | 2.58 | .975/.975/.933/1.0 |
| 3 | 08-29 | + 粗品类精确匹配加分（镜像评测器 coarse_category，开场句的 X 就是目标 categories 尾部） | **0.8607** | 0.980 | 0.664 | 2.42 | .975/.988/.967/1.0 |
| 3a | 08-29 | 消融：ASK_POLICY=entropy（熵策略替代 other_first） | 0.8298 | 0.980 | — | 3.31 | 结论：other_first 对当前模拟器更优（other 每问必吐 2 条约束）|
| 4a | 08-29 | 分层抽样 40 条基准（USE_LLM=0，规则排序） | 0.8819 | 0.975 | 0.775 | 2.90 | 抽样口径，供 4b 对照 |
| 4b | 08-29 | 抽样 40 条：USE_LLM=1，deepseek-v4-flash listwise 精排 top-20（只喂标题+价格；温度未固定） | **0.7794** ↓ | 0.975 | **0.440** ↓ | 3.00 | **LLM 精排反而降分**：规则序吃"约束逐字命中"指纹信号，LLM 看不到命中证据、凭语义重排破坏了精确第一名。耗时 2m11s / 7.8 万 tokens / 约 ¥0.14。实测延迟 0.85s/调用 |
| 4c | 08-29 | 同 4b 但 temperature=0（已设为客户端默认） | **0.7969** ↓ | 0.950 | 0.548 | 3.13 | 负收益方向稳定（两轮 0.78~0.80），15/40 会话排名变差；public_0007 从"2 轮第 1 名"退化到第 6 名~miss。另证实 temp=0 下服务端仍有轻微不可复现性——**线上重排不可复现**本身就是对评测驱动开发的额外扣分项 |
| 5 | 08-29 | A-T1：override 点名约束重新提升为 hard（原 bug：重复值跳过，用户最强信号停留软权重）+ A-T2：无 "What I need is" 的兜底 override 句触发降权 | 0.86067 | 0.980 | 0.664 | 2.42 | 与 #3 **逐会话完全相同**（stash 对照，0/200 变化）——公开集零影响的纯正确性/私有集保险修复：T2 句式本地评测器不出现（behavior 恒有 message 键）；T1 的 hard 提升恰未翻转任何排名。见 team/A-任务清单.md |
| 5b | 08-29 | A-T3：归一化收归 `src/dialog/normalize.py`，Slot 新增 terms 字段，retriever/ranker 改为消费 slot.terms（匹配语义逐字对齐旧实现） | 0.86067 | 0.980 | 0.664 | 2.42 | 与 #5 逐会话 0/200 差异。**重要反结论（三镜头对抗审查实测，3000 商品/11793 约束）**：全文归一化串对 target norm_text 零失配（180 截断/内嵌分号全被标点折叠吸收）→ "键值剥离"类增配变体**零收益纯风险**（'Department: mens'→'mens' 子串命中全库 92.5%；实测激进版 2↑3↓ 净 -0.0003 已回退）。归一化的真实价值 = 架构收归 + 私有集内联改写卡的未来钩子（加变体须 ≥5 字符+词边界护栏，见 normalize.py 文档）。当时的 MRR 拆账（90/200 会话首命中排名>1、RR 损失 63.3、上限约 0.95）已被同晚 C-T1~T3 系列兑现（见 #8–#11） |
| 6a | 08-29 | M2 稠密路消融：bge-small-en-v1.5 + RRF(k=60) 平权融合（recall 口径，scripts/recall_at_k.py） | — | — | — | — | **负收益弃用**：Recall@100 0.995→0.970、avg_pos 25.6→30.2（⚠️ 口径：旧版脚本取"各轮**最差**位置"；后续 R@k 表改为"**最佳**位置"，两口径数字不可直接比较）。病因：稠密查询含语义噪声（"Imported"/礼物话术），噪声候选把 BM25 池 40-100 位挤出截断线 |
| 6b | 08-29 | M2 稠密路 v1：bge-small 预计算（data/embeddings.npz 72MB，scripts/precompute_embeddings.py 可重算）+ **非置换式并集融合**（BM25 top-100 不动，稠密独有候选追加队尾，budget 窗镜像生效）；USE_DENSE=1，资产缺失自动降级纯 BM25 | **0.86577** ↑ | 0.985 | 0.669 | 2.37 | .988/.988/.967/1.0；Recall@100 0.995→**1.000**（public_0020 被稠密路救回，全程未召回清零）。池内顺序不影响 rank() 打分，avg_pos 指标此后只看不比较 |
| 7 | 08-29 | M2 诊断：dense_sim 对排序打平局有无区分度（scripts/dense_signal_diagnostic.py，命中轮池内 sim 名次） | — | — | — | — | **阴性结论，route_scores 路线收掉**：rank1 组目标 sim 名次中位 3（50% ≤3），但 rank4-10 打平局里中位 81（仅 9% ≤3）——稠密与指纹信号同源（都被约束信息量驱动），打平局里语义一样瞎。MRR 长尾完整移交 M3 交叉编码器 |

| 8 | 08-29 | **C-T1 热度先验**：排序分加 `POP_WEIGHT × log10(1+rating_number)/5`（`POP_WEIGHT=1.5`） | **0.9180** ↑ | 0.990 | **0.815** ↑ | 2.07 | .988/1.0/.967/1.0 |
| 8a | 08-29 | C-T1 权重扫描（全量）：w=0 → 0.8607 ／ w=1.0 → 0.9128 ／ **w=1.5 → 0.9180** ／ w=2.0 → 0.9195 ／ w=6.0 → 0.9277 | — | — | — | — | 分数随 w **单调上升不见顶** = 过拟合警报（模型在学"公开集目标就是热门商品"这个采样事实）。故取保守 1.5 而非最高分；`POP_WEIGHT=0` 可一键关闭 |
| 8b | 08-29 | C-T1 难度分项（过拟合代理指标） | — | — | — | — | MRR：w=0 easy .627 / medium .641 / hard **.830** → w=1.5 easy .777 / medium .810 / hard **.928**。hard 桶同步上涨且 hit 不变（.967）→ **不是纯 easy 桶的采样假象**，先验有普适性 |
| 8c | 08-29 | **C-T1 对 M2 交接文档点名的"打平局"的作用**（回答实验 7 的遗留问题） | — | — | — | — | B 点名的 rank4-10 共 44 条：**26 条直升第 1 名、38 条名次改善（86%）、1 条变差**；原 4 个 miss 救回 2 个（含 DEMO.md 幕 6 当"纯规则天花板"讲的 public_0087 → rank 10）。**结论见下方"排序轴 vs 先验轴"** |
| 8d | 08-29 | C-T1 后的剩余战场 | — | — | — | — | rank1 106→**145**、rank2-3 46→31、rank4-10 44→**22**、miss 4→**2**。MRR 从 0.815 全提到 1.0 也只值 **0.055 总分**，提分空间已接近枯竭 |
| 9 | 08-29 | **叠加验证：C-T1 热度先验 + M2 稠密路（USE_DENSE=1）** | **0.927162** ↑ | **0.995** | **0.831** ↑ | 2.03 | 1.0/1.0/.967/1.0（boundary MRR 满分）。两机制补不同的洞：先验救的 2 个 miss 与稠密救的 public_0020 无交集，叠加后全量仅剩 **1 个 miss**（intent_override）。当前最优配置 = 默认全量评测 + USE_DENSE=1 |

| 10 | 08-29 | **C-T2 has_price 先验**：`price` 字段非空即加 `HAS_PRICE_WEIGHT`；同时 POP_WEIGHT 1.5→2.0（停止准则见 10c） | **0.9345** ↑ | 0.995 | **0.855** ↑ | 1.98 | rank1=154、miss=**1**（hard 桶 hit 首次 1.000）|
| 10a | 08-29 | 先验轴信号诊断：目标 vs 全目录的字段分布 | — | — | — | — | **有 price 字段：目标 89.0% vs 全目录 20.8%（4.3 倍）**。且与热度独立——低热度子集(rating_number<1000)里全目录 has_price 20.2% 而目标 86.3%，差 66 个百分点。其余：average_rating 4.40/4.20、price 值 24.5/22.9、len(title) 79/73 均无区分度 |
| 10b | 08-29 | 消融：features 条数先验（目标中位 8 条 vs 全目录 5 条） | 0.9176 / 0.9127 | — | — | — | **负收益弃用**（w=0.5 / w=1.0 均低于不加的 0.9180）。分布差异存在但与指纹信号冲突 |
| 10c | 08-29 | **权重停止准则**：POP×HAS_PRICE 联合扫描 + 难度分项交叉验证 | — | — | — | — | HAS_PRICE 在 1.0 处**收敛**（1.0→0.9307、1.5→0.9308）= 真二元判别器而非无界 hack。POP 侧：w=2.0 三桶齐涨（easy .846/medium .825/hard .972），**w=3.0 时 easy 涨到 .871 但 medium 掉到 .812** = 开始学公开集采样特征 → 止步 2.0。**本项目权重调参一律用此准则：只取三个难度桶齐涨的最大值** |

| 11 | 08-29 | **C-T3 低置信轮收窄推荐**（`EARLY_TOPK=1` / `EARLY_TURNS=2`）：第 1-2 轮只返回最有把握的 1 件，第 3 轮起给满 10 件 | **0.9620** ↑ | 0.995 | **0.964** ↑ | 2.23 | rank1 154→**189/200**；miss 仍为 1；四场景 hit .988/1.0/1.0/1.0 |
| 11a | 08-29 | C-T3 参数扫描 + 停止准则 | — | — | — | — | TOPK 侧：1→0.9546 ／ 2→0.9457 ／ 3→0.9420 ／ 5→0.9374（**只给 1 件最优**）。TURNS 侧（TOPK=1）：1→0.9546 ／ **2→0.9620** ／ 3→0.9545 ／ 4→0.9538。难度交叉：TURNS=2 三桶齐涨（easy mrr .846→.967、medium .825→.959、hard .972 不变）且 hit 全不退；**TURNS=3 时 easy hit .988→.975、medium hit 1.000→.989、miss 1→3 = 拐点** |
| 11b | 08-29 | C-T3 的机理（为什么"藏牌"反而赚） | — | — | — | — | **命中即终局**：第 1 轮以第 7 名命中会把 MRR 永久锁死在 1/7。单条会话对比：第 1 轮第 7 名 = 0.5+0.3×0.143+0.2×1.0 = **0.743**；第 2 轮第 1 名 = 0.5+0.3×1.0+0.2×0.9 = **0.980**。净赚 0.237/条。改动前有 **31 条**会话正是"第 1 轮命中但排 2-10 名"，全部被这一项救回。MTTC 1.98→2.23 的损失（-0.005 分）远小于 MRR 收益（+0.033 分） |

| 11c | 08-29 | **C-T3 收窄触发条件的三种实现对照**（公开集同分，选型看私有集鲁棒性） | 三者同为 **0.9620** | 0.995 | 0.964 | 2.23 | `turns`（写死轮次）／`slots`（看槽位数）／**`hybrid`（槽位不足【且】未超轮次上限，默认）** 在公开集上完全等价 |
| 11d | 08-29 | **降级鲁棒性证明**：模拟私有集改写导致解析全失效（`EARLY_MIN_SLOTS=99`，槽位永远填不满） | slots **0.9281** ↓↓ ／ hybrid **0.9545** | .950 / .985 | — | 2.60 / 2.35 | **纯 slots 模式无安全出口会永远只推 1 件 → HitRate 崩到 0.950**；hybrid 第 3 轮后自动恢复满 10 件，只掉 0.008。这是选 hybrid 而非 slots 的唯一理由——公开集看不出差别，私有集才见分晓 |

| 12a | 08-29 | ❌ 消融：**先验权重随证据衰减**（`PRIOR_DECAY`，effective = W/(1+D×命中约束数)）。动机：第 1 轮无证据只能靠先验，有证据后证据该压过先验 | 0.9612 / 0.9577 / 0.9508 ↓ | — | — | — | **单调负收益**（D=0.1/0.2/0.5）。结论：先验在有证据时依然有效——"这是不是真人会买的东西"与"匹不匹配这句话"确实是**两条独立的轴**，不该此消彼长。开关保留默认 0 |
| 12b | 08-29 | ❌ 消融：**收窄轮单独加强先验**（`EARLY_PRIOR_BOOST`，只影响第 1-2 轮） | 峰值 0.9629 (B=1.5) | 0.995 | 0.966 | 2.23 | **判为噪声不采纳**：+0.0009 ≈ 1-2 条会话翻转的量级（单条会话从第 2 名升第 1 名 = 0.3×0.5/200 = 0.00075）。B=2.0 起转负。正是停止准则要挡的过拟合，默认保持 1.0 |

| 13 | 08-29 | **M4 诊断**（回应 M2 交接文档"转 D 评估 profile 软偏好路线"；基线为本行提交时的 main，即实验 8 附近 0.918023，晚于此的 C-T2/T3 先验工作未参与本次判断）：剩余 miss，`lexicon.py` 的 profile 软偏好词表能否救回？逐案例核对命中词是否落在目标商品文案里 | — | — | — | — | **阴性，不投入 ranker.py 接线**：`public_0020`（buying）命中词 [cotton, fit]——但 "cotton" 已是显式 hard 约束、权重远高于任何软加权，profile 信号对它纯属冗余；`public_0144`（intent_override）命中词 [comfort, soft, fabric]——均为服装类目通用营销词，样本外几乎任何商品文案都含有，无区分度，加权反而可能抬高无关候选。两案例均不支持"profile 软权重能救回当前 miss"的假设，与实验 7"语义轴信号已被指纹信号吃满、打平局里无区分度"结论一致。结论：`lexicon.py`/`signals.py` 保留（已测试、零回归、`state.distilled` 供未来 LLM 路径读取），但不再投入 `retriever.py`/`ranker.py` 的规则打分接线——检索侧已 Recall@100=1.000（实验 6b）无需扩召回，排序侧现有证据不支持再加一个语义类信号 |
| 14 | 08-29 | **M4 Reflection 诊断**（验收标准③，同上基线口径）：`signals.py` 的"过滤已展示未命中候选"若接入 `ranker.py`，全量 200 条逐轮 A/B 对比（手动复现排序步骤，未改生产代码）排序会不会变、变好变坏各多少 | — | — | — | — | **第一版发现真实缺陷，已修复并重新验证**：naive 版（一旦 scenario 明确就拉黑历史展示项）347 次逐轮对比中 19 次改善 / 1 次恶化——恶化样本正是 `public_0087`（DEMO.md 幕 6 讲解案例）：browsing 场景第 1 轮弱信息猜测未命中，第 2 轮拿到新约束后排序本可把目标送回 rank 10，却被"第 1 轮展示过"直接永久拉黑，变成 rank 11。**根因**：本模拟器没有真实的用户拒绝信号，"展示未命中"只代表"当时信息不够"而非"用户不要"，排序会随新约束改善、被拉黑的目标可能本该翻身。**修复**：新增 `signals.actionable_rejections(session_id, min_stagnant=2)`，只有连续 ≥2 轮无新槽位且候选池 top-5 不变（复用已有 `stagnant_turns`）才启用过滤，逻辑是"排序已经卡住不动了才值得强制换血"。重新全量 A/B：408 次对比，**5 次改善 / 0 次恶化**。建议 C 参考 `team/M4-交接文档.md` 里的 diff 决定是否接入（预期收益小——HitRate 已逼近 1.0，多数 session 命中太早、根本来不及积累停滞，但至少确认零风险） |
| 15 | 08-29 | **M4 P1 诊断**（同上基线口径）：override 冲突感知降权（`demote_preferences` 从"无差别降权全部旧槽位"改为"只降权跟新披露约束同属性类型的旧槽位"，其余保持 hard）——先查数据面：30 条 intent_override 里 7 条（23%）披露过跨属性类型约束，理论上会被这个改动影响。临时改 `state.py`/`parser.py` 跑全量 A/B（跑完立即 `git checkout` 还原，未提交） | 0.918023（=当时基线） | 0.990（=当时基线） | 0.815（=当时基线） | 2.07（=当时基线） | **零效果，不建议投入**：四场景 hit/mrr/mttc **逐位与基线相同**，7 个"理论上会受影响"的样本里没有一个实际改变最终排序结果（大概率是热度先验/品类精确匹配/长约束加权等其他信号已经主导了这些样本的打分，属性降权与否不改变胜负）。不同于实验 14（有正收益、已修复安全性问题），这条经排查后确认是"听起来合理但无实证支持"，不再推给 A |
| 16 | 08-29 | **C-T6 改写压力测试**（新脚本 `scripts/paraphrase_stress.py`，不改 evaluator：import 官方 `evaluate()` + 包装 Agent 改写 user_message） | L0 0.9620 ／ **L1 0.7792** ／ L2 0.7585 ／ L3 0.7523 | — | — | — | **量化了全项目最大的未量化风险**。分层结论：L1（只改句式、约束原文一字不动）就掉 **-0.183，占全部损失的 87%**；L2/L3（再改约束取值）只多掉 0.028。→ **脆的是句式匹配，不是逐字匹配；逐字指纹信号本身相当结实**（编号 13→16：与 M4 诊断行撞号，按惯例续在后面，见 commit 1427633 先例） |
| 16a | 08-29 | C-T6 机理定位 | — | — | — | — | L1 下 `state.category` 完全丢失、`scenario=unknown`、**整句话被当成一条软约束塞进槽位**。ranker 的主信号是"约束原文作为连续子串命中商品全文"，一整句话永远匹配不上 → 逐字信号根本没机会发挥。**不是语义理解问题，是片段抽取问题** |
| 17 | 08-29 | **C-T7 载荷抽取兜底**（`parser._salvage`）：严格模板全部落空时，按"冒号载荷 / 分隔符切分 / 改需求信号 / 粗品类"四条规则把约束片段从陌生句式里捞出来。**纯规则、零 LLM、零依赖** | L0 **0.9620**（逐字节不变）／ **L1 0.9455** ／ **L2 0.9151** ／ **L3 0.8734** | .995/.980/.960/.950 | — | — | **救回 L1 +0.166 ／ L2 +0.157 ／ L3 +0.121**。设计上只在严格模板落空时触发，故公开集零影响——这是它能放心上线的前提 |
| 18 | 08-29 | **M4 补充诊断**：profile 软偏好在"改写压力测试"下会不会比正常措辞下更有用？动机——`lexicon.py` 的信号来自 `user_profile`（结构化字段，不受改写影响），假设是改写让逐字指纹信号失效时，这类"改写也不动"的信号相对价值会变大。临时把 `lexicon.profile_soft_terms` 接入 `retriever._query_terms`（扩召回）+ `ranker.score`（+0.3 软权重），跑 `scripts/paraphrase_stress.py --n 60` 对照（跑完立即 `git checkout` 还原，未提交） | 不接入 L0 0.9595／L1 0.9595／L2 0.9634／L3 0.9375；**接入后** L0 **0.9519** ↓／L1 **0.9211** ↓↓／L2 **0.9018** ↓↓／L3 **0.8673** ↓↓ | — | — | — | **假设证伪，且比实验 13 的"零效果"更差——是负效果，且改写越狠掉分越多**。根因有二：①**与 C-T3 的 `EARLY_TOPK` 收窄策略负交互**——收窄轮只给 1 件推荐，profile 软权重一旦把错的候选顶到第一名，这一轮的机会成本从"10 选 1 保底"变成"独苗全押错"，代价被放大；②改写削弱精确信号（`state.category`/逐字命中）后，噪声词的相对权重被动升高——命中的 profile 词本来就是实验 13 点过名的"无区分度营销通用词"（comfortable/soft/fabric 等），信号越干净的环境这些噪声词危害越小，改写把环境弄脏后噪声反而被放大。**结论比实验 13 更强烈：不仅不投入，且如果排序层未来做类似"收窄/押注"策略，任何新增的弱判别力信号都要先过这个交互测试，不能只看公开集常规路径的分数**|

| 15 | 08-29 | **C-T8 长约束分片匹配**（`FRAGMENT_WEIGHT=0.8`）：逗号分隔的多成分约束，除整串外额外登记各成分为低权重匹配串 | L0 **0.9620**（任何权重下逐字节不变）／ L2 0.9151→**0.9186** ／ L3 0.8734→**0.8865** | — | — | — | 收掉实验 14 遗留的 L3 缺口的 15%。权重取 0.8 而非略高的 1.2（仅多 0.001）：`length_bonus` 最低为 1.0，权重 >1.0 会让"分片命中"压过"短整串命中"，语义上说不通 |

## 已知残留问题（提分方向）

- 剩余 3 个 miss 的意图卡全是泛化约束（"cotton"+"Imported" 类），预算未进卡——判别信号不足，需要 profile 软偏好（M4）兜住尾部。（实验 6b 后 Recall@100 已 1.000，miss 全是排序层问题）
- MRR 0.669 是当前最大提升空间（权重 0.30）：命中轮的目标排名还不够靠前 → M3 的交叉编码器/LLM 精排的主战场。
- 稠密路 v1 已上（实验 6b）：非置换并集保证"捞全"。~~route_scores（dense_sim 进排序）~~ **已用诊断实验证伪**（实验 7：打平局里目标 sim 名次中位 81，稠密与指纹信号同源）——不再推进，MRR 长尾归 M3。
- **给 C 的明确挑战（实验 4b）**：LLM 精排目前是负收益，USE_LLM 默认保持 0。要让它转正，方向有三：
  ① prompt 里带上命中证据（哪条约束命中了哪个候选），别只给标题；
  ② 只在规则低置信时调用（top-1 无约束命中 / 分数打平时做 tie-break），别全量重排；
  ③ 或者改用 LLM 做约束解析/查询改写而非排序（面向私有集句式变体的鲁棒性，这才是规则解析器最脆的地方）。
